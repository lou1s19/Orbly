.build/
build/
models/*.bin
.DS_Store
*.wav

# Secrets: never in the repo (signing keys live in the keychain)
.env
.env.*
*.p12
*.pem
sparkle_private_key*

# Bundled Whisper engine: a binary, produced by
# scripts/build-whisper-engine.sh
vendor/

# Local index, does not belong in the repo
.repowise/
